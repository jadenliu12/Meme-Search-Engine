export interface HybridArgs {
    alpha?: number;
    query: string;
    vector?: number[];
}
export default class GraphQLHybrid {
    private alpha?;
    private query;
    private vector?;
    constructor(args: HybridArgs);
    toString(): string;
}
